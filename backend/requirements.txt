# DocMind AI Backend - Ollama only (no OpenAI)
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.18
# Pydantic 2.10.x breaks Semantic Kernel (Url import); use 2.9.x
pydantic>=2.5.0,<2.10.0
pydantic-settings>=2.0.0,<2.5.0

# Semantic Kernel with Ollama connector (chat: llama3.2 / mistral)
semantic-kernel[ollama]>=1.0.0,<2.0.0

# Ollama Python client (embeddings: nomic-embed-text)
ollama>=0.3.0

# Vector store
chromadb>=0.5.0,<1.1

# PDF extraction
pdfplumber==0.11.4
PyPDF2==3.0.1

# Token counting for chunking
tiktoken==0.8.0

# Environment
python-dotenv==1.0.1
